---
interface Props {
  href: string;
  class?: string;
}
const { href, class: className } = Astro.props;
---

<a
  href={href}
  target="_blank"
  rel="noopener noreferrer"
  class:list={["underline decoration-dotted underline-offset-2", className]}
  ><slot /></a
>
